Verify Python3 is installed on your system

pip install the dependencies

	pip install -r requirements.txt

DNA samples I used are stored in dna_samples/.
DNA alignment data is stored in dna_alignments/.
tables for studies 2 and 3 are stored in tables/.

To run the 3 studies, run

	python run_study.py

The results of running the studies are already stored in dna_alignments/.

To convert the dna_alignments data from above into table format, run

	python table.py

The results of running the tables are already stored in tables/.

