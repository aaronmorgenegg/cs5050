Verify Python3 is installed on your system

pip install the dependencies

	pip install -r requirements.txt

To run the empirical studies, run

	python run_study.py

This will output data to the appropriate data file.

To create a graph from this data, run

	python graph.py

This will output a graph to schoolbook_graph.png

The program has already been run and the resulting
files are included in the homework submission.
Refer to schoolbook_data.txt, divide_conquer_data.txt, 
fft_data.txt, and the comparison graph files.

The analysis writeup is in writeup.txt

